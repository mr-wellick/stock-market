//import { fetchSuccess } from "../Fetch-Data";
//import { msftData }     from "../Fetch-Data";

test("test correct action object is log for fetch success", () => {
    // First, reduce data down into array
    //let __data        = Object.entries(msftData);
    //let processedData = fetchSuccess(__data);
});