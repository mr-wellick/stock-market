//import { processData } from "../Utilities";
//import { msftData }    from "../Fetch-Data";

test("make sure data is processed correctly", () => {
    // Process data
    //let rawData = Object.entries(msftData);
    //let newData = processData(rawData);
    //let __keys  = Object.keys(newData);

    // Need to figure out how to test objects
});